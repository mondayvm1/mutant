---
project: mutant
stage: pre-planning (codebase mapped, project not yet initialized)
last_updated: 2026-03-02T00:38:07.021Z
repo: https://github.com/mondayvm1/mutant
---

<current_state>
Codebase mapped with GSD. Reservation system built and pushed. Waiting on Stripe + Resend API keys
before the form actually works end-to-end. Next step is /gsd:new-project to build the full roadmap.
</current_state>

<what_is_built>

- Landing page (React + Vite + Tailwind) — deployed to Vercel
- Reservation form (CTASection.tsx) — name, email, demo day picker
- Vercel serverless function (api/reserve.ts) — POST /api/reserve
  - Creates Stripe Customer on reservation
  - Sends notification email to renebetancourtiii@gmail.com via Resend
  - Sends confirmation email to customer via Resend
  - Duplicate detection (same email can't reserve twice)
- Demo Day dates: March 15, March 22, April 5, April 19 (hardcoded)
- GSD codebase map: .planning/codebase/ (7 documents)

</what_is_built>

<blockers>

1. STRIPE_SECRET_KEY not set in Vercel — form returns "Server not configured yet"
   Fix: dashboard.stripe.com → Developers → API keys → copy Secret key (sk_test_...)
   Add to: Vercel project → Settings → Environment Variables

2. RESEND_API_KEY not set in Vercel — same issue
   Fix: resend.com → API Keys → Create → copy key (re_...)
   Add to: Vercel project → Settings → Environment Variables

3. After adding both keys → Vercel dashboard → Deployments → Redeploy

4. Resend sender domain is still "onboarding@resend.dev" (shared test domain)
   — works for now but emails may land in spam
   — eventually verify a real domain in Resend and update api/reserve.ts lines 43 + 59

</blockers>

<remaining_todos>

## Immediate (unblocked once env vars are set)
- [ ] Add STRIPE_SECRET_KEY to Vercel env vars
- [ ] Add RESEND_API_KEY to Vercel env vars
- [ ] Redeploy on Vercel
- [ ] Test form end-to-end (submit a reservation, verify email arrives at renebetancourtiii@gmail.com)

## Next (project roadmap)
- [ ] Run /gsd:new-project to build full roadmap
- [ ] Admin page — view all reservations (currently only visible in Stripe dashboard)
- [ ] Real email domain — verify domain in Resend, update from: address
- [ ] Rate limiting on api/reserve.ts (currently open to spam)
- [ ] Input validation with Zod (package already installed)
- [ ] Demo day dates should be configurable, not hardcoded

## Future
- [ ] Stripe payment when units ship (Stripe Customers already created, just need to charge)
- [ ] Database for reservations (Supabase or PlanetScale)
- [ ] Analytics

</remaining_todos>

<decisions_made>

- Deployment: Vercel (serverless functions in api/)
- Payments: Stripe ($0 down — creates Customer record for future charge)
- Email: Resend (owner notification + customer confirmation)
- Demo Days: Hardcoded preset dates in CTASection.tsx + DEMO_DAY_INFO in api/reserve.ts
- No database — Stripe Customers are the reservation store for now
- Owner email: renebetancourtiii@gmail.com

</decisions_made>

<next_action>
1. Go to dashboard.stripe.com → get secret key
2. Go to resend.com → get API key
3. Add both to Vercel env vars → redeploy
4. Test the form
5. Then: /clear → /gsd:new-project to build the roadmap
</next_action>
